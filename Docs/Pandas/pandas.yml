DROP COLUMN : df = df.drop (CN, 1)
  - CN : 'COLUMN NAME'
 
NEW COLUMN (oldcol + calc) : df['newcol'] = df['oldcol'] * Expr

NEW DATAFRAME (cols & index) : df = pd.DataFrame (columns = collist, index = idlist)

FILTER BY COL VALUE : filterdf = df [df['c'] == val]
